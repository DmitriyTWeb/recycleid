import React from 'react';

const Scan = (props) => {
  return(
    <section className="scan">
      <button type="button" className="scan__shot-btn">Сканировать</button>
    </section>
  );
};

export default Scan;
